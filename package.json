{
  "name": "render-bare-server",
  "version": "1.0.0",
  "description": "Production Node.js Bare Server backend for Render deployment",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@tomphttp/bare-server-node": "^2.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
